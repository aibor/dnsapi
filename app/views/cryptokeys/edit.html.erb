<h1>
  <%= t('.title') %>
  <%= " #{t('c.for')} #{@cryptokey.domain.name}" if @cryptokey.domain %>
  <%= button_to 'Back', @domainmetadatum.domain ? @domainmetadatum.domain : domains_path,
    class: "btn btn-default", method: :get %>
</h1>

<%= render 'form' %>
