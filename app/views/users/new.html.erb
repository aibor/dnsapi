<h1>
  <%= t('.title') %>
  <%= @user.username %>
  <%= button_to (t('c.back')), users_path,
    class: "btn btn-default", method: :get %>
</h1>

<div class="container">
  <%= render 'form' %>
</div>
